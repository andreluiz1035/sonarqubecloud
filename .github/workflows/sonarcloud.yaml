name: SonarCloud

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  sonarcloud:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install coverage pytest

      - name: Run tests with coverage
        run: |
          coverage run --source=src -m pytest
          coverage xml -o coverage.xml --rcfile=coverage.ini

      - name: Normalize coverage paths
        run: |
          echo "Normalizando paths do coverage..."
          sed -i "s|/home/runner/work/.*/src|src|g" coverage.xml
          sed -i "s|/home/runner/work/.*/tests|tests|g" coverage.xml
          echo "Coverage corrigido:"
          head -n 20 coverage.xml

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=andreluiz1035_sonarqubecloud
            -Dsonar.organization=andreluiz1035
            -Dsonar.sources=src
            -Dsonar.tests=tests
            -Dsonar.python.coverage.reportPaths=coverage.xml
            -Dsonar.inclusions=src/**/*.py
            -Dsonar.test.inclusions=tests/**/*.py
